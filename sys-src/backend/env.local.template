# Copy the contents of this file to `env.local` and fill in the values.
# Make sure that the secret information never gets committed to the repo.

DB_KEY=insert database key here
SECRET_KEY=insert secret key here
